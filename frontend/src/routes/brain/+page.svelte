<script lang="ts">
  // Standalone Brain hub route. Same component is embedded inside Workspace
  // settings (#brain) so the two surfaces never drift. See $lib/brain/BrainHub.
  import BrainHub from '$lib/brain/BrainHub.svelte';
</script>

<BrainHub />
