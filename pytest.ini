[pytest]
markers =
    e2e: end-to-end tests that require a running container (skipped automatically if /api/health is unreachable)
    rls_isolation: Track C1 — cross-tenant RLS isolation checks (require running container)
    rate_limit: Track C3 — rate-limit middleware regression checks
