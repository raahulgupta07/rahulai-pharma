{{- if .Values.ontologyCluster.cronEnabled }}
{{- $fullName := include "dash.fullname" . -}}
{{- $ns := .Values.namespace -}}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ $fullName }}-ontology-cluster-daily
  namespace: {{ $ns }}
  labels:
    app: dash
    component: ontology-cluster
spec:
  schedule: {{ .Values.ontologyCluster.schedule | default "0 3 * * *" | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  startingDeadlineSeconds: 3600
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: {{ default 1800 .Values.ontologyCluster.activeDeadlineSeconds }}
      template:
        metadata:
          labels:
            app: dash
            component: ontology-cluster
        spec:
          restartPolicy: OnFailure
          containers:
          - name: trigger
            image: curlimages/curl:8
            command: ["/bin/sh","-c"]
            args:
            - |
              set -e
              echo "[ontology-cluster] dispatching run-now"
              curl -fsS -X POST \
                -H "Authorization: Bearer ${ADMIN_TOKEN}" \
                "${DASH_API}/api/ontology/cluster/run-now" \
                --max-time 1500 \
                || { echo "[error] ontology cluster run failed"; exit 1; }
              echo "Done."
            env:
            - name: DASH_API
              value: "http://{{ $fullName }}-api.{{ $ns }}.svc.cluster.local:8000"
            - name: ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $fullName }}-secrets
                  key: ADMIN_API_TOKEN
            resources:
              requests: {cpu: 100m, memory: 128Mi}
              limits:   {cpu: 500m, memory: 256Mi}
{{- end }}
