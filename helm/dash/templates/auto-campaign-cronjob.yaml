{{- if .Values.autoCampaign.cronEnabled }}
{{- $fullName := include "dash.fullname" . -}}
{{- $ns := .Values.namespace -}}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ $fullName }}-auto-campaign-daily
  namespace: {{ $ns }}
  labels:
    app: dash
    component: auto-campaign
spec:
  schedule: {{ .Values.autoCampaign.schedule | default "0 2 * * *" | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  startingDeadlineSeconds: 3600
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: {{ default 1800 .Values.autoCampaign.activeDeadlineSeconds }}
      template:
        metadata:
          labels:
            app: dash
            component: auto-campaign
        spec:
          restartPolicy: OnFailure
          containers:
          - name: trigger
            image: curlimages/curl:8
            command: ["/bin/sh","-c"]
            args:
            - |
              set -e
              echo "[auto-campaign] dispatching cycle-all"
              curl -fsS -X POST \
                -H "Authorization: Bearer ${ADMIN_TOKEN}" \
                "${DASH_API}/api/projects/auto-campaign/cycle-all" \
                --max-time 1500 \
                || { echo "[error] auto-campaign cycle-all failed"; exit 1; }
              echo "Done."
            env:
            - name: DASH_API
              value: "http://{{ $fullName }}-api.{{ $ns }}.svc.cluster.local:8000"
            - name: ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $fullName }}-secrets
                  key: ADMIN_API_TOKEN
            resources:
              requests: {cpu: 100m, memory: 128Mi}
              limits:   {cpu: 500m, memory: 256Mi}
{{- end }}
